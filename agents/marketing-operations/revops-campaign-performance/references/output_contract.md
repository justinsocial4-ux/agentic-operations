# Output Contract

Return exactly one helper-rendered JSON document. It contains policy and population receipts, row evidence states without metric numbers, one numeric receipt per approved measurement group, limitations, and three false authorization flags.

Sort all lists deterministically. Keep unresolved rows visible. Do not add prose, Markdown, rankings, duplicated numbers, qualitative labels, recommendations, or a second rendering. The helper owns the explicit empty terminal line.
