# Research And Claims

The legacy research never received independent QA: Section 18 is blank. Its self-check misstates line count, source quality, population consistency, and dependency status.

Do not repeat its conversion, follow-up-speed, labor, pipeline leakage, forecast variance, match accuracy, attribution-window, event-tier, sample-size, completeness, cost threshold, confidence, benchmark, model, budget uplift, or ROI claims as facts/defaults.

Primary platform documentation supports configurable event and attribution records. It does not validate causal sourcing or universal performance thresholds. Primary incrementality guidance uses treatment/control or calibrated causal evidence to estimate lift; absent that evidence, return `INCREMENTAL_ROI_UNAVAILABLE`.
