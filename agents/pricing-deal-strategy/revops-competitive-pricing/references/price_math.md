# Price Math

Accept positive plain Decimal strings only. For an approved comparable pair, calculate `left amount - right amount`. Calculate percentage difference only as that difference divided by the positive right amount, multiplied by one hundred, under the customer-approved scale and rounding mode.

Each source amount, difference, and percentage belongs in one helper receipt. Other sections refer to receipt IDs and never repeat a number.
