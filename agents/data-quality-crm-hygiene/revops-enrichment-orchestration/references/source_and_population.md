# Source and population

- Bind every source version to exact schema, authorization window, query, page, pseudonymization, and complete-population receipts.
- Declare the complete pseudonymous record population and one exact requested field set per record.
- Every route receipt must appear exactly once in one source population and exactly once in the route plan.
- Missing, duplicate, extra, future, partial, or unauthorized evidence fails closed. Never query a system to fill a gap.
